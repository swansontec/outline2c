#include <stdio.h>

@o2c include "test.ol";

/* List the settings in a single location: */
@o2c outline settings {
  number width;
  number height;
  number threads;
  string username;
  string save_dir;
}

/* Generate a structure to hold the settings: */
typedef struct {
  @o2c for setting in settings with number { int setting; }
  @o2c for setting in settings with string { char *setting; }
} Settings;

/* Generate a function to save the settings: */
void save_settings(Settings *s, FILE *f)
{
  @o2c for setting in settings with number {
    fprintf(f, "%s=%d\n", setting!quote, s->setting); }
  @o2c for setting in settings with string {
    fprintf(f, "%s=%s\n", setting!quote, s->setting); }
}

int main(int argc, char *argv[])
{
  return 0;
}
